# M7 移动端 LLM 远程控制 —— 交接文档（2026-08-10）

> **这份文档的用途**：让下一个会话**不必重读对话记录**就能接着写。
> 它讲「现在在哪、下一步干什么、以及本轮新学到的坑」。
>
> **与 `M7-交接-2026-08-09.md` 的分工**（那份**不要删，仍然要读**）：
>
> | 那份文档的节 | 状态 |
> |---|---|
> | §4 已拍板的决策（K1–K17 摘录 + 三条 08-09 拍板） | **仍然权威**，接手前必读 |
> | §5 踩过的坑（环境 / Windows / 协议兼容 / 白名单铁律 / Claude 实测事实） | **仍然权威**，最有价值的一节 |
> | §6 搁置的 `main.rs` 重构（解剖数据 + 被推翻的方案） | **仍然有效**，别重做解剖 |
> | §7 工作方法（实现 → 对抗验证 → 修复） | **仍然有效**，本轮延续了它并证明了它有用 |
> | §9 两个 HUD bug | **已修完**，那一节现在记的是根因与结论 |
> | §1 当前状态 / §2 已完成分片 / §3 剩余分片 / §10 零碎 | **已被本文取代** |
>
> **权威指针**：设计蓝图 `docs/M7-移动端LLM远程控制-设计蓝图-2026-08-08.md`（4292 行）。
> 设计细节一律去查蓝图，**不要凭本文的转述做决定**。
>
> **两条铁律照旧**：
> 1. 协议的权威定义永远是 `crates/lumen-protocol/src/llm.rs` 本身，蓝图 §5.11 是草案、可能落后；
> 2. PC → 手机**必须白名单转发**，绝不能黑名单过滤或原样透传（理由见 08-09 那份 §5.4，
>    那是四次连续翻车换来的）。

---

## 1. 当前状态

**分支** `feat/m7-protocol`。

```
ad4a206  docs(M7): 交接文档 2026-08-10 —— 片1 已闭合，下一步片5 的开工说明
eb25509  feat(M7): 片1 Dart 半边 —— 手写协议模型 + 读同一批 golden 语料双向断言
587ba10  fix(hud): 上下文语义按段判定，并补上 Claude 的 5 小时/本周用量
1c0c09c  docs(M7): 交接文档
998781b  feat(M7): 片0b —— Flutter 移动端工程地基与 CI
fe7e686  feat(M7): 片4/4b —— LLM 数据面打通、能力握手、隐藏会话控制面
3ce16f7  feat(M7): 片1/2/3 —— golden 语料、headless runner 底座、Claude 适配器
dc7fcc6  feat(M7): 移动端 LLM 远程控制的协议地基与设计蓝图
6bc48e7  fix: pin f32 in five Stroke::new calls to unbreak clippy CI   ← 与 M7 无关，修 CI 红
```

⚠ **片 5 的改动在工作区里，尚未提交**（见 §2.3 的文件清单）。全部验证已跑绿。

**尚未合并到 `main`。**

### 验证基线（接手第一件事就跑这个）

```bash
cd F:\ClaudeWorkSpaces\lumen
cargo test -p lumen-protocol                             # 48 + 11 passed（片 5 把 golden 从 7 加到 11）
cargo test -p lumen-app                                  # 1045 passed
cargo test -p lumen-server                               # 41 passed / 6 ignored
cargo clippy --workspace --all-targets -- -D warnings    # CI 口径，必须零警告

cd mobile
./tool/test.sh --analyze                                 # flutter analyze --fatal-infos，零问题
./tool/test.sh                                           # 230 passed（片 1 时是 88）
```

⚠ **Flutter 侧一律走 `tool/test.sh` / `tool/test.ps1`，别裸敲 `flutter test`**——
本机常驻 http 代理会让它**无输出卡死**（脚本里 `env -u` 把代理变量从子进程环境摘掉）。
`flutter pub get` 同理，走 `tool/test.sh --pub`（它会在 pub get 之后把 `pubspec.lock`
里的镜像 URL 归一回 `pub.dev`，否则 CI 的 `--enforce-lockfile` 第一步就红）。
**片 5 又在这两个脚本里补了一道**：非 `--pub` 分支现在会把 `PUB_HOSTED_URL` 钉回源站，
理由见 §5.5——那是本轮踩到的、最隐蔽的一个坑。

### 已落地的文件

| 文件 | 说明 |
|---|---|
| `crates/lumen-protocol/src/llm.rs` | 归一化 LLM 子协议，**权威定义** |
| `crates/lumen-protocol/tests/golden/mobile/` | **89 条**双语言语料（61 帧 + 6 c2s + 14 s2c + 7 rest + 1 enums）+ **契约说明书 README.md** |
| `crates/lumen-protocol/tests/mobile_golden.rs` | Rust 侧 11 个断言（片 5 从 7 个扩到 11） |
| `crates/lumen-app/src/remote_ws/llm.rs` | 数据面收发、握手、流控、清理点 |
| `crates/lumen-app/src/llm_runner/**` | 进程 / 行解析 / 白名单门 / Claude 适配器 |
| `crates/lumen-app/src/llm_cli.rs` | 终端画面刮取（HUD 的模型 / 上下文 / 额度） |
| `mobile/lib/protocol/**` | Dart 协议层，**8 个文件**（片 1 的 5 个 + 片 5 的 rest_dto / remote_c2s / remote_s2c） |
| `mobile/lib/{core,net,data,state}/**` | **片 5 新增**：见 §2.3 |
| `mobile/test/**` | **230 项断言**（片 1 是 88） |

---

## 2. 本轮（2026-08-10）做完了什么

### 2.1 两个 HUD bug（与 M7 无关，海风哥 08-09 报的）

细节在 08-09 那份 §9，那一节已改写成结论。这里只留一句根因，因为它是**可复用的教训**：

- **上下文语义反了**，根因不是关键词表写错，是**匹配范围**错——老代码拿**整行**做子串匹配，
  而状态行是多个互不相干的指标拼在一起的一整行，额度那半边的「重置**剩余**」跨段命中了
  上下文的语义判定。修法是把判定单位从「整行」降到「分段」。
- **Claude 的 5 小时 / 本周用量**：数据一直画在同一行上，只是没解析。补了状态行刮取，
  额度是 **JSON 优先、画面兜底**，且画面刮取只对 Claude 开。

★ **一条通用教训**：从终端画面刮取的判定，**判定单位必须是「段」不是「行」**。
误命中的表现是「数值对、语义反」——比彻底读不到更难发现。

### 2.2 片 1 的 Dart 半边（M7 主线）

`mobile/lib/protocol/` 五个文件，**零 Flutter 依赖**（只用 `dart:core` / `dart:convert`）：

| 文件 | 职责 |
|---|---|
| `codec.dart` | 带字段名的类型化读取器、`Option` 与 `default` 的两套写出规则、UTF-8 夹紧、开放式 C 型枚举、内部标签枚举骨架 |
| `llm_enums.dart` | 10 个开放式 C 型枚举（`Known \| Other(原文)`） |
| `llm_model.dart` | 块 / 用量 / 对话元信息 / 轮记录 / 增量项 / 审批 + 三个脱敏包装 |
| `llm_frame.dart` | `LlmFrame` 30 个变体，未知 `op` 降级成 `LlmUnknown` |
| `remote_frame.dart` | 外层信封（externally tagged，只实现移动端用得到的子集） |

`mobile/test/protocol/golden_test.dart` 88 项，逐条实现契约 §2 与 §3：往返 + 不动点 +
变体覆盖矩阵 + 十个开放枚举的 `Other` + 外层信封 + 脱敏渲染 + 字段取值断言。

**至此协议层两侧对齐**：改协议时 Rust 与 Dart 会一起红。

### 2.4 片 6 之一：扫码配对的载荷协议与四重校验（2026-08-11）

`crates/lumen-protocol/src/pairing_qr.rs` + `mobile/lib/protocol/pairing_qr.dart` +
6 条 `qr_*` 语料。载荷**不上线缆**（只经屏幕→摄像头），进协议 crate 的唯一理由是两端必须
逐字节一致。四重校验每种拒绝对应**一条不同的 UI 文案**，其中 `ForeignServer` 是钓鱼信号，
**绝不提供「是否切换服务器」的选项**。

两条容易写错、各有语料守着的：**形状先于身份**（魔数不对报 Malformed，不能报身份不符）、
**过期时刻只做软提示永不硬拒**（服务端 TTL 才是权威，时钟偏差不得造成可用性故障）。

⚠ PC 侧渲染与手机三页 UI **未完成**，原因分别见 §5.7 与 §3。

### 2.3 片 5：移动端网络与控制面（M7 主线，**本轮第二块**）

验收目标「手机能登录、能看到设备列表」所需的**全部非 UI 部分**已落地，UI 三页是片 6。

**协议层补齐（语料先行）**

| 新增 | 内容 |
|---|---|
| 语料 28 条 | `c2s_*` 6 条（手机会发的全部变体）、`s2c_*` 14 条（**Rust 侧全集**）、`rest_*` 7 条、`enums_control_plane.json` 1 条 |
| `mobile_golden.rs` | 用例信封改成 `frame` / `c2s` / `s2c` / `rest` / `enums` **五选一**；新增 `RemoteC2S` / `RemoteS2C` 与四个 C 型枚举的变体表；新增 4 个测试（控制面往返、控制面覆盖、枚举全集、REST 往返） |
| `mobile/lib/protocol/` | `rest_dto.dart`（含 `LumenRoutes` 路径常量）、`remote_c2s.dart`、`remote_s2c.dart`；`codec.dart` 加 `readClosedEnum` |

**★ 控制面与数据面的口径相反，这是本片最需要交接的一条**：内层 `LlmFrame` 有 `#[serde(other)]`
兜底（未知 `op` 降级），外层 `RemoteC2S` / `RemoteS2C` 是**外部标签枚举、不支持 `other`**
——未知变体就是整条 `from_str` 失败。于是覆盖要求也相反，且**两侧不对称**：

- **S2C 必须全实现（14 个）**。手机是接收方，收到一条没实现的变体就是整条消息丢失。
  其中 5 条是「手机理论上收不到」的镜像消息，照样实现——「理论上」是对服务端**当前行为**的
  假设，不是协议保证（`DeviceKind` 是 P1 的软策略）。
- **C2S 刻意只实现 6 个**。`RequestControl` / `DeclineControl` / `EndSession` / `Relay`
  不实现，手机在类型层面就发不出去。`mobile_golden.rs` 的 `手机不发的C2S` 常量逐条写着理由，
  覆盖断言要求「有语料的 ∪ 刻意不发的 == 全集」且两者不相交。

**新增的应用层**

| 目录 | 文件 | 要点 |
|---|---|---|
| `core/` | `result.dart` / `env.dart` / `log.dart` | `canonicalServerOrigin` 与 `cloud.rs` **同一批测试用例**；反斜杠要显式拒（两套 URL 解析器口径不同） |
| `net/` | `backoff.dart` / `scheduler.dart` / `ws_client.dart` / `io_ws_socket.dart` / `rest_client.dart` / `net_error.dart` | 见下 |
| `data/` | `device_identity.dart` / `token_store.dart` | `hw_id` 三个细节（hex 小写 / `0x00` 分隔符 / UTF-8）与桌面端逐字节一致 |
| `state/` | `link_controller.dart` / `device_list_controller.dart` / `providers.dart` | 显式 sealed `LinkState` + **12 秒发起超时** |

**与桌面端刻意不同的三处都落地并测到了**：指数退避 + 全抖动（下界 50%）、25s Ping + **15s
Pong 看门狗**、后台 25s 宽限。定时器与 socket 全部可注入（`FakeScheduler` / `FakeWsSocket`），
所以这四条时间线在纯 Dart 里就能推进虚拟时钟测完，含「刚好没到 / 刚好到了」的边界。

**测试**：88 → **230 项**。新增 `control_plane_golden_test` / `rest_golden_test` /
`ws_client_test`(22) / `link_controller_test`(25) / `rest_client_test`(13) /
`backoff_test` / `env_test` / `device_identity_test`，公共部分抽进 `test/protocol/corpus.dart`
与 `test/support/`。

**变异测试**（按 08-09 §7 的纪律，收工前做）：七处关键通路各打断一次，**全部被抓住**——
`writeOpt` 改成无条件写出、退避下界改回 0、拆掉 Pong 看门狗、收到回音后不取消发起超时、
`hw_id` 去掉 `0x00`、续期失败后不清凭据、S2C 未知变体宽容成兜底。

> ⚠ 其中一次变异的 `sed` 引号转义炸了、命令根本没执行，脚本却报了「变异后仍全绿」——
> **变异测试自己也会假阳性**。看到「没被抓住」时，先确认那次变异真的改到了文件。

---

## 3. 剩余分片（P0）

| 片 | 内容 | 前置 |
|---|---|---|
| **6** | 登录 / 设备列表 / 配对三页 + 扫码（PC 侧出二维码）。**载荷协议与四重校验已完成**（两端 + 6 条语料）；三页 UI 未开工；PC 侧二维码渲染写好但被 §5.7 的工具链问题挡住 | 三页 UI 无前置；扫码卡在 §5.7 |
| 7 | 对话骨架 + 流式渲染（可先用本地 JSONL 回放做假数据源解耦） | 片 5（已完成） |
| 8 | PC 生命周期（session-id/resume、空闲回收、transcript 补齐）+ 桌面图标与审计 | 片 4（已完成） |
| 9 | 工具卡片 + diff（**用 `tool_use_result` 的结构化数据，不要用 `tool_result.content` 字符串**） | 片 7 |
| 10 | 持久化与离线补齐（sqflite 三表 + outbox 乐观发送/去重） | 片 7 |
| 11 | 服务端硬化：背压、idle 超时 + WS Ping、默认密钥拒启动、`device_pairs` 撤销 API、登录节流 | 独立 |
| 12 | 打包与真机验收 | 全部 |

已完成：片 0、0b、1（含 Dart 半边）、2、3、4、4b、**5**。

---

## 4. 下一步：片 6（登录 / 设备列表 / 配对三页 + 扫码）

**验收目标**：手机扫 PC 屏幕上的二维码即完成配对；扫**别的服务器**的码显示安全警告。

### 4.1 蓝图里对应的段落（先读这些，别凭本文转述）

| 内容 | 位置 |
|---|---|
| 分片表里片 6 那一行（含 PC 侧要新建的两个文件） | 蓝图 :3927 |
| 控制面状态机与页面的对应关系 | 蓝图 §7.5（:3116 起） |
| 移动端应用锁与免码的**强制绑定**（片 6 要落地的产品规则） | 蓝图 §9.3（:3875 起） |
| 服务端既有空白（无背压 / 无 idle 超时 / 45 秒在线窗口） | 蓝图 :3426、:4240 |

### 4.2 片 5 交给你的东西（直接用，别重写）

| 要做的事 | 用什么 |
|---|---|
| 输服务器地址 | `ServerEndpoint.tryParse(用户输入)`，null 即非法；`isInsecure` 判明文 HTTP |
| 注册 / 登录 | `RestClient.register/login`，登录成功**自动写 TokenStore** |
| 设备列表 | `DeviceListController.refresh()` + `states` 三态；**不要重排** |
| 建立会话 | `LinkController.open(target)` → `submitCode(code)` → `state` 变成 `LinkActive` |
| 页面路由 | `switch (linkState)` 直接出页面（sealed，漏一个编译不过） |
| 数据面 | `LinkController.sessionFrames`（已按 `sessionId` 过滤），载荷是不透明 JSON，自己 `RemoteFrame.fromJson` 并**单独兜异常** |

**两个注入点在片 6 落地**（`providers.dart` 里现在是能在测试里跑的默认实现）：
`tokenStoreProvider` 换成 Keychain / Keystore 实现，`rawMachineIdProvider` 换成
`device_info_plus`。用 `ProviderScope(overrides:)` 在 `main.dart` 覆盖，
**别把平台实现设成默认值**——那样 `flutter test` 就得起 platform channel。

### 4.3 四件必须做对的事

1. **★ 不要做「记住这台电脑、下次免码」的 UI。** 隐藏会话**每次都要念码**，
   这是片 4b 的止血（理由见 §5.6）。UI 上给这个预期，用户才不会觉得是 bug。
2. **12 秒超时的文案指向版本，不指向网络。** `LinkFailure.serverTooOld` 已经判好了，
   UI 要给可操作提示：「请确认 PC 上的 Lumen 正在运行、已登录，且版本不低于 v1.1」。
   同理 `DenyReason.offline` 也要覆盖「版本过低」——服务端在目标未上报 `hidden` 能力位时
   复用了这条拒因（新增拒因会打掉老客户端唯一的回执）。
3. **十个 `DenyReason` 各有文案，两个不能笼统**：
   - `AlreadyControlled` 在隐藏桶里是「该电脑的手机会话已达上限（**2**）」，
     **必须给具体数字**——只说「已被占用」会让用户去找一个并不存在的占用者；
   - `TargetPairing` 是「该电脑正在与另一台设备配对，请稍候」，**不是**「已被占用」
     （这条文案 §6 从 08-09 起就挂着待改，片 6 一起清掉）。
4. **生物锁与免码的强制绑定**（§9.3）：关掉生物锁 = 调 `DELETE /api/v1/pairs/*` 清空本机
   全部信任对。⚠ **那个撤销 API 现在还不存在**（片 11 才补），所以片 6 落地时要么先只做
   「关生物锁 = 本地删 token + 提示用户去 PC 上删设备」，要么把片 11 的这一条提前。
   **不要静默地只关生物锁**——那正是「把一个已知会塌陷的组合交给用户」。

### 4.4 二维码那条链路（片 6 的另一半，在 PC 侧）

`crates/lumen-protocol/src/pairing_qr.rs` 与 `crates/lumen-app/src/remote_pairing_qr.rs`
**都还不存在**，要新建。扫码要携带 origin，手机端**必须比对**：扫到的 origin 与本机已登录的
不一致时显示安全警告而不是静默切换服务器——切过去就是把凭据发给另一台服务器。

### 4.5 写 Dart 协议类型时必须遵守的四条（已经被测试钉死）

1. `Option` + `skip_serializing_if` 的字段，`toJson` 里必须让键**整个消失**，
   用 `writeOpt()`；而带 `default` 但**不带** skip 的字段相反，输出里一定要出现。
   两者混淆是手写模型最常见的漂移。
2. 整数字段**只认 `int`，不接受 `double`**。Dart 的 `num` 相等跨 `int`/`double`
   （`240262 == 240262.0` 为真），所以一处存成 `double` 几乎不会被往返断言发现。
3. **未知内容降级但不回显**，不设 `extra` map。任何「先收下来、以后再说」的容器
   都是白名单转发的后门。
4. **（片 5 新增）分清哪一层有兜底**：内层 `LlmFrame` 与它嵌套的那些 tagged 枚举有
   `Unknown` 降级；**外层 `RemoteC2S` / `RemoteS2C` 与那四个 C 型枚举没有**——它们是外部标签
   枚举，未知变体/未知取值都是整条报废，两端都必须**抛**而不是宽容。把这一层「顺手加个兜底」
   反而与 Rust 不一致，`control_plane_golden_test.dart` 有断言守着。

---

## 5. ★ 本轮新增的坑（08-09 那份没有的）

### 5.1 往返断言对「同型字段两侧同时接反」完全免疫——这是实测的，不是推理

把 `known_seq` 与 `known_turn` 在**解码与编码两侧同时**互换，65 条语料**全部照绿**，
只有契约 §3 第 7 条那组**取值断言**把它抓住了。

⇒ **给已有变体加同型字段时，必须回去给 `golden_test.dart` 末尾那组取值断言补一行**，
否则那个字段永远没人测。语料已经刻意给相邻同型字段配了互不相同的取值，就是为了让
这类断言写得出来。

### 5.2 「一次就全绿」在手写协议模型上是警报，不是好消息

88 项第一次跑就全通过。按 08-09 §7 的纪律做了变异测试（故意打断代码看测试会不会红）：

| 打断什么 | 结果 |
|---|---|
| `writeOpt` 改成无条件写出（即所有可选字段都写 `null`） | **21 项红** |
| `known_seq`/`known_turn` **只在解码侧**接反 | 2 项红（往返断言抓到了） |
| 同两个字段**在两侧对称**接反 | **65 条语料全绿，只有 1 项红**（见 5.1） |

⇒ **测试写完要做变异测试再收工。** 三次变异各自验证了一条通路真的被测着。

### 5.3 状态行刮取的判定单位是「段」不是「行」

见 §2.1。同一类错误在 Dart 侧的对应物是「相邻同型字段」——两者的共同点是
**错得很像对的**，不会报错、只会长期失真。

### 5.7 ★★ 两个会话同时编译 → rustc 进程内存损坏（2026-08-11，**已定位**）

**根因：两个 Claude Code 会话并发对同一个 `target/` 跑 cargo。**另一个会话结束后，
`cargo build -p lumen-app` 立刻恢复（16.34s 通过），改动一行没动。

这条值得记下来，因为**它伪装成了代码问题**，害我排查了三轮：

| 观察到的现象 | 当时的（错误）推论 |
|---|---|
| 加 `qrcodegen` 就崩，撤掉就好 | 「是这个 crate 的问题」 |
| 换成 `qrcode` 还是崩 | 「是 lumen-app 依赖数量到临界了」 |
| 撤掉全部新依赖仍然崩 | 「是 rustc 自身 bug / 硬件 / 杀软」 |

三次崩溃给出**三种不同错误码**（`STATUS_ACCESS_VIOLATION` 0xc0000005、
`STATUS_HEAP_CORRUPTION` 0xc0000374、`STATUS_STACK_BUFFER_OVERRUN` 0xc0000409），
而 `cargo check` **每次都干净通过**——现在回头看，「三种内存错误 + check 通过 +
撤销改动也不稳定复现」这个组合本身就该指向**进程外的干扰**，而不是某一处代码。

⇒ **判据**：`cargo build` 崩在 codegen 且错误码在几次之间变来变去时，
**先确认没有第二个会话/终端在跑 cargo**，再去看代码。cargo 的文件锁挡得住同一个
crate 被编两次，但挡不住两个 rustc 进程同时读写 `target/debug/incremental` 与 PDB。

> 顺带一条协作纪律：多个会话同时开工时，**别让两边都跑 cargo**。Flutter 侧
> （`mobile/`）与 `lumen-protocol` 这类小 crate 不受影响，可以并行。

现象：给 `crates/lumen-app/Cargo.toml` 加一个新依赖后，`cargo build -p lumen-app` 在
codegen 阶段崩溃：

```
error: could not compile `lumen-app` (bin "lumen")
process didn't exit successfully: rustc ... (exit code: 0xc0000005, STATUS_ACCESS_VIOLATION)
                                            (或 0xc0000374, STATUS_HEAP_CORRUPTION)
```

**已做的排除**（结论：不是代码问题，也不是某个特定 crate 的问题）：

| 试了什么 | 结果 |
|---|---|
| `cargo check -p lumen-app --all-targets` | ✅ **通过**（只有未接线的 dead_code warning）⇒ 类型层面没问题，崩在 codegen |
| 换库：`qrcodegen 1.8` → `qrcode 0.14` | ❌ 一样崩 ⇒ 不是某个 crate 的锅 |
| 清 `target/debug/incremental` | ❌ 一样崩 ⇒ 不是增量缓存损坏 |
| 撤掉那一个依赖 | ✅ **立刻恢复**（5.87s 编译通过） |
| 内存 | 63.7 GB 总 / 36.8 GB 可用 ⇒ **不是 OOM** |
| `RUSTFLAGS="-C debuginfo=1"` 全量重编 | ❌ 连 `wgpu-hal`、`lumen-ssh` 这些**与改动无关**的 crate 也崩 |
| **撤掉全部新依赖后再编** | ❌ 仍然崩（第三种错误码）⇒ 不是「加依赖」引起的 |
| `cargo check -p lumen-app` （撤掉依赖后） | ✅ 2.39s 通过 ⇒ 代码本身是好的，只有 codegen 崩 |
| **另一个会话结束后再编** | ✅ **16.34s 通过** ⇒ 根因就是并发编译 |

最后一行值得单独记：全量重编时无关 crate 也崩，说明这台机器上的 rustc 在**并行编译大 crate**
时本身就在稳定性边缘，而 `lumen-app` 加一个依赖只是把它推过了线。

**没有验证过的候选解法**（都影响全项目，需要海风哥拍板）：

1. **升级 rust toolchain**（当前 stable-x86_64-pc-windows-msvc）——最可能是上游已修的 bug；
2. `[profile.dev] debug = 1`——减小调试信息规模，代价是断点调试体验；
3. 降低并行度（`cargo build -j 2`）或换 linker（lld）；
4. 把杀软对 `target/` 的实时扫描排除掉——Windows 上这类「随机 ACCESS_VIOLATION」的常见来源。

**在它解决之前**：`crates/lumen-app/src/remote_pairing_qr.rs` 已经写好（含 4 条测试）但
**没有 `mod` 声明、不参与编译**。解决之后只需要两步：`main.rs` 加一行 `mod remote_pairing_qr;`、
`Cargo.toml` 加一行依赖。**先跑一次 `cargo build -p lumen-app` 确认基线是绿的再动手**，
否则会把工具链问题误判成自己写错了。

> 附带的产品选项：扫码在 K13 里是「P0 做」，但它只是 9 位数字码的**另一种呈现**——
> 数字码那条路径本来就在。真要绕开这个阻塞，可以先发「手输 9 位码」的版本，
> 扫码等工具链修好再补。**这是海风哥的决定，不是可以顺手替他做的。**

### 5.4 `mobile/` 的静态分析比想象的严

`analysis_options.yaml` 开了 `strict-casts` / `strict-inference` / `strict-raw-types`，
CI 跑 `flutter analyze --fatal-infos`，**info 级也阻塞合并**。踩到过两条：

- `always_use_package_imports`：`lib/` 下的文件互相 import **必须用 `package:lumen_mobile/...`**，
  不能用相对路径（`test/` 下的文件互相 import 不受这条约束，片 5 的 `test/protocol/corpus.dart`
  与 `test/support/*` 就是相对 import）；
- `avoid_renaming_method_parameters`：重写 `operator ==` 时参数名必须叫 `other`，
  与字段同名时用 `this.other` 消歧。

### 5.5 ★ 一次「只读」的 `flutter analyze` 悄悄改掉了 146 行 `pubspec.lock`

本机把 `PUB_HOSTED_URL=https://pub.flutter-io.cn` 设成了**全局**环境变量，而
`flutter analyze` / `flutter test` 都会**隐式**跑一次 pub get。pub 把 hosted URL 当作依赖
身份的一部分：环境里的 URL 与 lock 里的对不上时，它会**忽略 lock 重新解析整个依赖图**，
顺手把依赖升到最新兼容版本（实测 `test` 1.26.3 → 1.30.0），再把镜像 URL 写回 lock 的
每一条 `url`。

后果是 CI 的 `flutter pub get --enforce-lockfile` 第一步就红，而**本地看不出来**：
diff 里只是一堆 URL 和版本号，很容易当成「谁顺手升了个依赖」。

老写法只在 `--pub` 分支归一化 lock，`analyze` / `test` 这两条**更常跑**的路径完全不设防。
片 5 已在 `tool/test.sh` 与 `tool/test.ps1` 的非 `--pub` 分支把 `PUB_HOSTED_URL` 显式钉回
`https://pub.dev`（不是摘掉——摘掉会退回默认值，效果一样但看不出意图）。

⇒ **判据**：跑完任何 flutter 命令后，`git status mobile/pubspec.lock` 应当是干净的。
不干净就是又踩了这条。

### 5.6 ★ 蓝图 §7.5 的「已配对免码直连」对隐藏会话**不成立**（以服务端代码为准）

蓝图写「已配对设备跳过配对码直达 `LinkActive`（`hub.rs:387-390` 的 `if paired`）」——
那对**镜像**会话成立，对**隐藏**会话不成立。片 4b 落地时做了止血，两处一起看才完整：

- `ws.rs` 的 `OpenHidden` 臂**恒传 `paired = false`**；
- `hub.rs::submit_pairing` 在 `ControlKind::Hidden` 分支**恒返回 `None`**（⇒ 不写 `device_pairs`）。

理由是 `device_pairs` 只有 `(user_id, dev_lo, dev_hi)`、**没有会话种类列**，两类会话共用一行
信任会双向越权：手机为「跟 LLM 说话」念的那次码会**同时**授予它日后免码建**镜像**会话的能力
（全屏 + 远程输入）；反向则让一台早年为镜像配过对的桌面端免码开一条**隐藏**会话——而隐藏会话
在被控端**没有横幅、没有指示器、没有结束入口**，等于把有 UI 的通道换成同等权力、零指示的通道。

⇒ **手机端每次都要念码**，UI 不要做「记住这台电脑」。
⇒ 片 5 的 `LinkController` 仍保留 `LinkRequesting → LinkActive` 的直接跃迁（服务端那条
`if paired` 分支还在，将来加了 kind 列就会走活），但**不能假设它会发生**。

同源的第二条：**断线即会话消失**。服务端 `disconnect` 立刻 `teardown_hidden_for_device`，
没有重连续挂——蓝图里那个 `LinkReattaching` 状态因此**没有实现**（留一个永远到不了的状态，
只会让后人以为有重连续挂机制）。

---

## 6. 未合并 / 待办的零碎

- **片 5 的改动尚未提交**（工作区），本分支也**尚未合并到 `main`**。CI 修复（`6bc48e7`）可以单独先合。
- UI 文案待改：手机隐藏配对进行中时，桌面控制端请求同一台 PC 会收到 `TargetPairing`，
  现有文案若是「已被占用」会误导，应改成「该电脑正在与另一台设备配对，请稍候」。
  （片 6 做手机端文案时一起清掉，见 §4.3-3。）
- **片 5 留给片 6 的两个注入点**：`providers.dart` 里 `tokenStoreProvider` 与
  `rawMachineIdProvider` 目前是能在测试里跑的默认实现（内存 / 返回 null），
  真机实现要用 `ProviderScope(overrides:)` 覆盖，**别改默认值**。
- **`DELETE /api/v1/pairs/*` 还不存在**（片 11），而 §9.3 的「关生物锁 = 清空信任对」
  依赖它。片 6 落地生物锁时要么先做「本地删 token + 提示去 PC 删设备」，要么把片 11 那条提前，
  **不要静默地只关生物锁**。
- `llm.rs` 里有**两个测试**函数超了 200 行门槛，该拆：
  `llm_全变体满载往返`（401 行）与 `llm_样本b实测行可被本协议表达`（203 行）。
  生产代码零超标，只有这两处测试。
- `device_pairs` **没有撤销 API**（只能靠删设备触发外键级联）。
  手机一次配对 = 对 PC 的长期免码控制权，**丢手机即失控**。移动端上线前必须补
  「已信任设备枚举 + 撤销」，且 `LUMEN_JWT_SECRET` 必须确认已设强随机值
  （当前默认密钥只 `warn` 不拒绝启动）。这两条在片 11。
- HUD 侧有几条**已知未修**的残留风险（措辞表之外的说法会被默认判成「已用」、
  上下文画面刮取绝对优先永不回退到 JSON 等），逐条记在 08-09 那份 §9 的「残留风险」表里，
  改 HUD 之前先读那张表。
- **仍未取到的 Claude 实测样本**：`redacted_thinking` 真样、`system/status` 行、
  `isReplay:true` 的回显行、`Bash`/`Edit`/`Write` 的 `tool_use_result` 形态、
  `can_use_tool` 控制请求、`--resume` 语义、`permission_denials[]` 元素结构。
  清掉需要在**普通终端**（非 Claude Code 内，嵌套子进程必 403）再采一次样，
  配方见蓝图 §12。采样**不许用 PowerShell 的 `>` 重定向**（会按 GBK 毁掉 UTF-8 且不可逆）。
