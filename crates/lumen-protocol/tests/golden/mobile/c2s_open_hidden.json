{
  "note": "手机发起一条到 target 的**隐藏会话**（不是镜像会话）。老服务端不认识这个变体，整条 from_str 失败后只记一条 debug 就继续读——**完全无回音**，所以手机端必须自带 12 秒发起超时并把文案落在「服务器版本过低」而不是「网络错误」。手机**绝不能**改发 RequestControl：那条会被老服务端当普通镜像请求执行成功，凭空占住那台 PC 的镜像独占位且两端都察觉不到。",
  "expect_variant": "OpenHidden",
  "c2s": {
    "OpenHidden": {
      "target": "pc-1"
    }
  }
}
